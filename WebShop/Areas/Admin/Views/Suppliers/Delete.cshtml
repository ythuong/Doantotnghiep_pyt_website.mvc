@model WebShop.Models.Supplier

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="page-header">

    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Home</a>
            <a class="breadcrumb-item" asp-area="Admin" asp-controller="Suppliers" asp-action="Index">Danh sách nhà cung cấp</a>
            <span class="breadcrumb-item active">Thông tin nhà cung cấp: @Model.Name</span>
        </nav>
    </div>
</div>
<div class="card">
    <div class="card-body">
        <h4 class="card-title">Bạn có muốn xóa nhà cung cấp: @Model.Name</h4>
        <div class="table-responsive">
            <table class="product-info-table m-t-20">
                <tbody>
                    <tr>
                        <td>ID:</td>
                        <td class="text-dark font-weight-semibold">@Model.SupplierId</td>
                    </tr>
                    <tr>
                        <td>Tên nhà cung cấp:</td>
                        <td>@Model.Name</td>
                    </tr>
                    <tr>
                        <td>Địa Chỉ:</td>
                        <td>@Model.Address</td>
                    </tr>
                    <tr>
                        <td>Số lượng hàng cung cấp:</td>
                        <td>@Model.ProductQuantity</td>
                    </tr>
                    <tr>
                        <td>Xác nhận xóa:</td>
                        <td>
                            <form asp-controller="Suppliers" asp-action="Delete" asp-route-id="@Model.SupplierId">
                                <input type="hidden" asp-for="SupplierId" />
                                <button type="submit" class="btn btn-danger btn-tone"><i class="far fa-trash-alt m-r-5 fa-lg"></i>Xác nhận xóa</button>
                                |
                                <a class="btn btn-success btn-tone m-r-5" asp-action="Index"><i class="fas fa-times m-r-5 fa-lg"></i>Hủy bỏ</a>

                            </form>
                        </td>
                    </tr>
                </tbody>
            </table>
            @*<div class="card-footer text-center">
            <form asp-action="Delete">
            <input type="hidden" asp-for="ProductId" />
            <input type="submit" value="Xác Nhận Xóa" class="btn btn-danger" /> |
            </form>

            <a asp-controller="AdminProducts" asp-action="Index" class="btn btn-danger">
            <i class="fa fa-chevron-left">&nbsp;</i>Quay lại
            </a>
            </div>*@
        </div>
    </div>
</div>
@*<div>
    <a class="btn btn-primary btn-tone m-r-5" asp-action="Index">Quay lại</a>
</div>*@
